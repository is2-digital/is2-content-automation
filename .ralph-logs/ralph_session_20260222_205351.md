--- Iteration 1 ---






Let me get the details on the first task.



Let me find all references to `curated_articles` in the codebase.



Let me read the key files that need modification.







Now let me read the test files and the repository file.



Let me also check for any other test files referencing these.


Now I have a clear picture of all the files that need changes. Let me start with the model rename.

**Files to modify:**
1. `ica/db/models.py` — rename `CuratedArticle` → `Article`, table `curated_articles` → `articles`, add `type` column
2. `ica/db/crud.py` — update import and references
3. `ica/db/__init__.py` — update re-exports
4. `ica/db/migrations/versions/001_initial_tables.py` — update table name, add `type` column
5. `ica/pipeline/article_curation.py` — update import and references
6. `ica/pipeline/article_collection.py` — update docstring
7. `ica/db/repository.py` — no changes needed (doesn't reference CuratedArticle directly)
8. `tests/test_pipeline/test_article_curation.py` — update comments

Let me make all the changes.




Now update the CRUD module and `__init__.py`.


Now update `__init__.py`.


Now update the migration.



Now update the pipeline code.



Now update the docstring in article_collection.py.


Now update the test file comments.




Now let me also check if there are any test files for DB models/crud that reference the old names.

Good, all test references are updated. Now let me also update the CLAUDE.md reference.



Now let me run the tests to verify everything works.



All 1767 tests pass. Let me do a final verification that no stale references remain in source code.

No remaining references in the `ica/` source directory. Now let me update the todo list, close the bead, update activity.md, and commit.

Now update activity.md and close the bead.



Now commit and push.







The pre-commit hook updated `.beads/issues.jsonl`. I need to commit that too.


Branch is up to date with origin. Now check for remaining open tasks.

Open tasks remain. Stopping here per the single-task constraint.

**Summary: ica-tru.1 complete**

Renamed `curated_articles` → `articles` with a `type` discriminator column across the codebase:

| File | Change |
|---|---|
| `ica/db/models.py` | `CuratedArticle` → `Article`, `__tablename__` → `"articles"`, added `type: Mapped[str]` with `server_default="curated"` |
| `ica/db/crud.py` | Updated import + all query references |
| `ica/db/__init__.py` | Re-exports `Article` instead of `CuratedArticle` |
| `ica/db/migrations/versions/001_initial_tables.py` | Table name + `type` column in `create_table` / `drop_table` |
| `ica/pipeline/article_curation.py` | Import + type hints |
| `ica/pipeline/article_collection.py` | Docstring |
| `tests/test_pipeline/test_article_curation.py` | Comments |
| `CLAUDE.md` | Database table reference |

All **1767 tests pass**. Pushed to origin.

--- End of iteration 1 ---
--- Iteration 2 ---




The sandbox is blocking the `bd` command. Let me retry without sandbox restrictions.


Let me get the details on the first non-epic task.


Let me claim this task and then understand what needs to be done.





Now let me read the existing codebase to understand models, CRUD, and related modules.









Now let me look at the n8n summarization subworkflow to understand the data prep logic.






Let me also check the date parser and existing pipeline structure.





Now I have a clear understanding of the task. Let me also check for existing test patterns.


Good, I have a clear picture of the testing patterns. Now let me implement the solution. First, I need to add a `parse_date_mmddyyyy()` function to the date parser, then create the summarization pipeline module.




Now let me create the summarization pipeline module.

Now let me write the tests.


Now let me run the tests.


All 69 tests pass. Let me now run the full test suite to ensure nothing is broken.

All 1836 tests pass (1767 existing + 69 new). Now let me update the activity log and commit.


Now close the task and commit.







The `bd sync` staged the issues.jsonl file. I need to commit it and then push.


Successfully pushed. Let me verify the git status is clean and check for remaining open tasks.

Branch is up to date with origin. Now checking for remaining open tasks:

There are remaining open tasks. Stopping here as instructed — one task per iteration.

---

**Task completed: ica-duo.26 — Implement summarization data preparation**

**What was done:**
- Created `ica/pipeline/summarization.py` — ports the first half of the n8n `summarization_subworkflow.json`:
  - `filter_approved_rows()` — filters sheet rows to `approved=yes` (n8n "Fetch Data from Sheet" filter)
  - `normalize_article_row()` — converts string fields to typed `CuratedArticle` (n8n "Field Mapping" Set node)
  - `upsert_curated_articles()` — PostgreSQL INSERT...ON CONFLICT upsert with `type='curated'` (n8n "Structure SQL Insert Query" Code node)
  - `prepare_summarization_data()` — orchestrates: get LLM config → fetch sheet → filter → normalize → upsert
- Added `parse_date_mmddyyyy()` to `ica/utils/date_parser.py` — reverse of `format_date_mmddyyyy`
- Created `tests/test_pipeline/test_summarization.py` — 69 tests

**Test results:** All 1836 tests pass (1767 existing + 69 new)

--- End of iteration 2 ---
--- Iteration 3 ---






Let me get details on the first actionable task.


Good. Task `ica-tru.2` is a refactor to rename `newsletter_themes` to `themes` — similar to `ica-tru.1` which renamed `curated_articles` to `articles`. Let me claim it and get started.





Let me read the key files that need modification.




Now let me read the pipeline files that reference `NewsletterTheme`.



Now let me check the test files.



Let me also check the prompts file for references.



Now let me make all the changes. Starting with the model.



Now update CRUD operations.



Now update the migration file (only the `newsletter_themes` table, not the `newsletter_themes_user_feedback` table).


Now update `db/__init__.py`.

Now update the pipeline files.


Now update `CLAUDE.md`.



Now let me verify there are no remaining `NewsletterTheme` references in the `ica/` source (excluding `NewsletterThemesUserFeedback` which is the feedback table and stays unchanged).





All 1836 tests pass. Let me also verify there are no remaining `NewsletterTheme` references in tests.

No remaining references. Let me also verify no `"newsletter_themes"` string references remain in `ica/` source (excluding `newsletter_themes_user_feedback`).

All clean. Now let me close the task, update activity.md, commit, and push.


Now update activity.md and commit.


Now commit and push.





The `bd sync` updated `.beads/issues.jsonl` but it wasn't committed. Let me commit it.

Pushed successfully. Let me verify and check for remaining open tasks.



Task `ica-tru.2` is complete. Here's a summary of what was done:

**Renamed `newsletter_themes` table to `themes` with type discriminator**

Changes made:
- **`ica/db/models.py`** — Renamed `NewsletterTheme` class to `Theme`, table `newsletter_themes` → `themes`, added `type` column (`String(50)`, `server_default='newsletter'`)
- **`ica/db/crud.py`** — All `NewsletterTheme` references → `Theme`
- **`ica/db/__init__.py`** — Re-exports `Theme` instead of `NewsletterTheme`
- **`ica/db/migrations/versions/001_initial_tables.py`** — Table name updated, `type` column added
- **`ica/pipeline/theme_selection.py`** — Docstring references updated
- **`CLAUDE.md`** — Database table name reference updated

All 1836 tests pass. Open tasks remain — stopping as per single-task constraint.

--- End of iteration 3 ---
--- Iteration 4 ---




The bd command is failing due to sandbox restrictions. Let me retry.


I see several tasks available. The highest priority concrete task is `ica-tru.3` (priority 0). Let me get its details.


Good. The task is to consolidate 4 `*_user_feedback` tables into a single `notes` table with a `type` discriminator. Let me claim it and start working.







Now let me find all references to the feedback models and table names across the codebase.




Let me also read the pipeline files and check for any other references to the feedback table names in CLAUDE.md and the test files.




I now have a complete picture. Let me also check what the theme_selection.py and theme_generation.py reference around lines 680+ and 220+.




Now let me check the relevant test assertions to understand how to update them.






Good, I have a complete picture now. Let me also check for DB CRUD tests and the CLAUDE.md database reference.



Now I'll implement the changes. The plan:
- Replace 5 feedback models + `FeedbackMixin` with a single `Note` model having a `type` discriminator
- Type values: `user_summarization`, `user_newsletter_themes`, `user_markdowngenerator`, `user_htmlgenerator`, `user_email_subject`
- `Note` has: `id`, `feedback_text`, `type`, `newsletter_id` (nullable), `created_at`
- Update CRUD to pass `note_type` string instead of model class
- Update all references

Now replace the 5 feedback classes with the single `Note` model:


Now update `crud.py`:

Now update the feedback functions:


Now update the migration:

Update the downgrade:


Now update `db/__init__.py`:


Now update the pipeline modules. First, `theme_generation.py`:

Now update the `aggregate_feedback` type hint and the `generate_themes` function:

Now update the docstring and body of `generate_themes`:

Now update `theme_selection.py`:

Now update the `store_theme_feedback` function call:

Now let me also update the docstrings in the prompt modules that reference old table names:





Now update the tests. First `test_theme_selection.py`:

Now update the test that asserts `call_args.args[1] is NewsletterThemesUserFeedback`:


Now update the integration test that patches `add_feedback`:


Now check if `Note` import is actually used (since we removed the assertion that references `NewsletterThemesUserFeedback`):

The `Note` import is no longer used in the test file since we replaced the `is NewsletterThemesUserFeedback` assertion with a string comparison. Let me remove it:

Now update `test_theme_generation.py`:

Now update the mock patches in test_theme_generation.py from `get_recent_feedback` to `get_recent_notes`:


Now update `CLAUDE.md`:





Now let me run the tests to see if everything passes:


All 1836 tests pass. Let me do a final sweep to make sure there are no remaining references to the old names in the `ica/` source:



One remaining docstring reference to the old function name. Let me fix it:

Let me also check for stale references in the tests directory:

That's just a test method name `test_add_feedback_option` which tests a Slack UI constant, not a DB function — it's fine.

Let me run tests one more time to confirm everything still passes after the docstring fix:

All 1836 tests pass. Now let me update `activity.md` and commit.



Now let me commit and push:



Good — net reduction of 115 lines. Let me check recent commit messages for style, then commit:






The beads JSONL was staged but not committed by `bd sync`. Let me commit it:


Branch is up to date with origin. Now let me check for remaining open tasks:


Open tasks remain — stopping as instructed.

**Task ica-tru.3 complete.** Here's what was done:

**Consolidated 5 feedback tables into a single `notes` table:**

- **`ica/db/models.py`** — Removed `FeedbackMixin` and 5 feedback classes (`SummarizationUserFeedback`, `MarkdownGeneratorUserFeedback`, `HtmlGeneratorUserFeedback`, `NewsletterThemesUserFeedback`, `NewsletterEmailSubjectFeedback`). Added single `Note` class with `type` discriminator column (values: `user_summarization`, `user_newsletter_themes`, `user_markdowngenerator`, `user_htmlgenerator`, `user_email_subject`) and composite index on `(type, created_at)`.

- **`ica/db/crud.py`** — Replaced generic `add_feedback(session, model, text)` / `get_recent_feedback(session, model)` with `add_note(session, note_type, text)` / `get_recent_notes(session, note_type)` that filter by `Note.type`.

- **Migration** — Replaced 5 separate table creates with single `notes` table.

- **Pipeline** — Updated `theme_generation.py` and `theme_selection.py` to use new CRUD functions with `"user_newsletter_themes"` type string.

- **Tests** — Updated mock patches and assertions. All **1836 tests pass**.

- Net **-115 lines** (111 added, 226 removed).

--- End of iteration 4 ---
--- Iteration 5 ---






I see several available tasks. Let me get details on the highest-priority task-level item.



Let me read the relevant PRD section and the existing summarization code in parallel.







Now let me look at the n8n summarization subworkflow to understand the per-article loop, HTTP fetching, and failure detection.






Now let me look at the existing tests and relevant utility code.



Now let me check what the PRD Section 4.1 says about the summarization prompt format (already have `summarization.py`), and look at the learning data extraction prompt to understand the patterns.



Let me check if there are HTML-to-markdown dependencies already available.


Now I have a thorough understanding of the n8n workflow. Let me implement the per-article summarization loop. I'll extend the existing `summarization.py` with the loop logic.

Now I'll write the implementation. Let me update `summarization.py` with the per-article loop.


Now let me add the new data types and functions after the existing code.

Now let me add the helper functions and the main loop after the `prepare_summarization_data` function.


Now let me verify the implementation compiles correctly, then write tests.



Good. Now let me write the tests.

Now let me run the tests.


All 98 tests pass. Now let me run the full test suite to make sure I haven't broken anything.

All 1934 tests pass (1836 existing + 98 new).

Now let me update `activity.md` and commit.


Now let me close the bead task and commit.












Task **ica-duo.27** is complete. Pushed to `origin/main`. Here's what was done:

**Implemented:** Per-article summarization loop (`ica/pipeline/summarization.py`)

- **HTTP page fetching** with browser-like headers (User-Agent, Accept, Referer, etc.) matching the n8n "Fetch Page Content" node
- **Fetch failure detection**: error messages, captcha marker ("sgcaptcha"), YouTube URL check — matching the n8n "If" condition node
- **Slack manual fallback** (`SlackManualFallback` protocol) for when HTTP fetch fails — matching the n8n "Manual Article Content" sendAndWait node
- **HTML-to-text conversion** (`strip_html_tags`) removing scripts/styles, stripping tags, unescaping entities
- **Learning data fetch** from `notes` table (last 40, type `user_summarization`) via `get_recent_notes`
- **Feedback aggregation** into bullet-point list (`• text`) matching the n8n "Aggregate Feedback" Code node
- **LLM call** via `litellm.acompletion` with the summarization prompt
- **Output parsing** via regex for URL/Title/Summary/Business Relevance matching the n8n "Format output" Code node
- **Sequential loop** (`summarize_articles`) processing articles one at a time like n8n `splitInBatches`

**New data types:** `FetchResult`, `ArticleSummary`, `SummarizationLoopResult`  
**New protocols:** `HttpFetcher`, `SlackManualFallback`  
**Tests:** 98 new, 1934 total passing

--- End of iteration 5 ---
--- Iteration 6 ---






The next actionable task is `ica-duo.28`. Let me get the full details.



Let me read the relevant PRD section and the existing code in parallel.




Now let me look at the n8n workflow, existing related code, and prompt modules I'll need.






Now let me check the LLM config for summarization-related purposes and the CRUD functions.



Now let me look at the n8n summarization workflow for the Slack output formatting and feedback loop details.


Now I have all the context I need. Let me also quickly check the existing n8n workflow for the "Share summarized content" node to understand how the blocks are sent.


I now have a complete understanding of the task. Let me implement the summarization output formatting and feedback loop. I need to add:

1. **Slack formatting** — mrkdwn text + Block Kit blocks for summary display
2. **Form builders** — next-steps dropdown form (Yes / Provide Feedback / Restart Chat)
3. **LLM calls** — regeneration + learning data extraction
4. **DB storage** — learning feedback → notes table
5. **Main orchestrator** — `run_summarization_output()` with feedback loop


Now let me add all the new code after the existing `summarize_articles` function.


Good. Now let me write the tests.

Let me check the existing test file structure and the test patterns used.

Good, now I know the test patterns. Let me write the tests for the new output/feedback functionality.

Now let me run the tests.



All 98 new tests pass. Now let me run the full test suite to make sure nothing is broken.


All 2032 tests pass (1934 existing + 98 new). Let me now close the task and commit.

Now let me update the activity log and commit.


Now let me commit and push.







The beads JSONL needs to be committed.




Now let me check for remaining open tasks.

Open tasks remain. Stopping as instructed — the next iteration will pick up the next task.

---

**Task completed: ica-duo.28 — Implement summarization output and feedback**

**What was done:**
- Added to `ica/pipeline/summarization.py`:
  - `SlackSummaryReview` protocol (3 methods: channel message, form, freetext)
  - `SummarizationOutput` dataclass (articles, text, model)
  - Slack constants matching n8n node configs (header, field labels, messages, dividers)
  - `format_summary_slack_text()` — builds mrkdwn text with header, count, per-article details
  - `build_summary_slack_blocks()` — builds Slack Block Kit section+divider pattern
  - `build_next_steps_form()` — dropdown: Yes / Provide Feedback / Restart Chat
  - `parse_next_steps_response()` — maps form response → `UserChoice`
  - `summaries_to_output_articles()` — PRD Section 5.2 output format
  - `call_regeneration_llm()` — feedback-based regeneration via `SUMMARY_REGENERATION` model
  - `extract_summary_learning_data()` — learning data extraction with JSON parsing
  - `store_summarization_feedback()` — notes table insert (`user_summarization`)
  - `run_summarization_output()` — main orchestrator with feedback/restart loop using `conditional_output_router`
- Created `tests/test_pipeline/test_summarization_output.py` (98 tests)
- All 2032 tests pass

--- End of iteration 6 ---
Reached max iterations (6)
